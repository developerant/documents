### NVM

nvm install stable

Latest version: 
```bash
nvm install node --reinstall-packages-from=node
```

Stable (LTS) version: 
```bash
nvm install lts/* --reinstall-packages-from=node
```

Set nvm version: 
```bash
nvm use <npm version>
```